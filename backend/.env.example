# Anthropic API key (required for LLM reconciliation)
ANTHROPIC_API_KEY=your-api-key-here

# Optional API key to protect endpoints (leave blank to disable auth)
API_KEY=

# Comma-separated allowed origins for CORS
CORS_ORIGINS=http://localhost:5173
